# Address of the deployed MidnightFactory (see contracts/deployments/<network>.json)
NEXT_PUBLIC_FACTORY_ADDRESS=

# 97 = BSC Testnet, 31337 = local hardhat node
NEXT_PUBLIC_CHAIN_ID=97

# Optional RPC override (defaults to the chain's public RPC)
NEXT_PUBLIC_RPC_URL=
