# RPC endpoint for BSC Testnet (default public endpoint used when unset)
BSC_TESTNET_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545

# Deployer private key (never commit the real one)
PRIVATE_KEY=
